# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.ipynb_checkpoints/

# Data is versioned by DVC (it manages data/.gitignore; .dvc pointer files ARE committed).
# Models / checkpoints stay out of git.
*.sif
checkpoints/
*.pth
*.ckpt

# Experiment tracking (MLflow local store)
mlruns/
mlartifacts/
mlflow*.db

# Env / secrets
.env
*.local
# HPC job scripts / container defs — cluster-specific, kept local only
scripts/hpc/
logs/

# OS
.DS_Store

# Claude
CLAUDE.md

# Personal notes (interview prep / problem log) — not committed
info.md

# Gradio
.gradio